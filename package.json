{
  "name": "web3-frontend-portfolio",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*",
    "contracts/crowdfunding-contract"
  ],
  "scripts": {
    "test": "yarn workspaces run test",
    "build": "yarn workspaces run build"
  },
  "devDependencies": {
    "typescript": "^5.0.4",
    "@types/node": "^20.2.5"
  }
}